# JoeOS shared packages

- `ui`: semantic design tokens, widget chrome, state surfaces, and accessibility behavior.
- `shared`: versioned domain, API, event, and configuration contracts.
- `sdk`: clients for queries, intents, streams, offline cache, and outbox synchronization.

Packages must be platform-neutral where practical so TypeScript, Python, and Swift representations can be generated or tested from one contract.
